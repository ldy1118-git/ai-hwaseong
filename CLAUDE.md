# Mars-Fit — 작업 메모

화성시 소상공인이 자기 조건에 맞는 지원사업을 찾고 **신청 완료까지**
안내받는 서비스. 2026 AI화성 챌린지 출품작.

배포: https://ai-hwaseong-ten.vercel.app

폴더 구조·API 명세·실행 방법은 `README.md` 에 있다. 여기 옮겨 적지 말 것.
두 군데 적으면 한쪽만 낡는다.

---

## 반드시 지킬 것

**이 저장소는 public 이다.** 주소만 알면 누구나 코드를 본다.

- **키를 커밋하지 않는다.** `.env` 는 gitignore 되어 있다. 실제 키는
  Vercel 환경변수와 GitHub Secrets 에만 넣는다. 프론트에서 `VITE_` 로
  시작하는 변수에 키를 넣으면 빌드 결과물에 평문으로 박힌다.
- **개인정보를 커밋하지 않는다.** `backend/users/` (기기별 온보딩 답변),
  사업자등록증 이미지. 둘 다 gitignore 되어 있다.
- **self-hosted GitHub 러너를 붙이지 않는다.** public 저장소라 아무나
  fork 해서 PR 하나로 그 서버에서 코드를 돌릴 수 있다.

## git

브랜치를 따로 파지 않는다. `main` 에 바로 푸시한다. 셋뿐이고 파일 담당이
갈려 있어서, 브랜치는 충돌을 없애주는 게 아니라 뒤로 미룰 뿐이다.

    git pull --rebase             # 작업 시작 전
    cd project && npm run build   # 푸시 전. 여기서 걸리면 남의 배포가 안 깨진다
    git push

예외 — 여러 파일에 걸치고 하루 넘게 걸리는 작업은 브랜치를 판다.

푸시하면 GitHub Actions 가 빌드를 검사한다(`.github/workflows/build-check.yml`).
빨간불이면 그 커밋은 화면을 못 띄운다는 뜻이다. `project/` 가 바뀐 커밋에만 돈다.

## 공고 자동 갱신

기업마당 API 가 GitHub 러너의 IP 에 응답하지 않는다. 그래서 수집은
**연구실 서버의 cron** 이 맡는다. 매일 06:11(KST).

    scripts/cron_update_notices.sh     실제 로직 (전용 클론 mars-fit-cron 에서 돈다)
    ~/bin/mars-notices.sh              cron 이 부르는 런처. 손으로 돌릴 때도 이것
    mars-fit-cron-logs/last-run.txt    마지막 결과 한 줄

### 공고는 두 군데서 온다

    기업마당 API        policy_data/collect.py --raw       주 소스
    화성시청 고시공고     policy_data/hscity.py --support    화성시 전용 사업

**기업마당에 안 올라오는 화성시 사업이 있다.** 소상공인 자금지원사업(특례보증
5천만원 + 이차보전 2% 5년)과 저신용 소상공인 미소금융 이자지원이 그렇다.
둘 다 지금 열려 있는데 목록에 없었다.

`--support` 는 목록 앞 몇 장만 훑는다. 946페이지를 매일 다시 긁으면 11분이
걸리는데 새 공고는 앞쪽에만 붙는다. 대신 받아둔 것을 **저장소에 커밋한다.**

    policy_data/hscity_support.json    ← raw/ 가 아니다. 커밋되어야 한다

`raw/` 에 두면 gitignore 돼서, cron 이 `reset --hard` 로 시작하는 전용
클론에서 매일 첫 실행처럼 앞 몇 장만 본다. 1월에 떠서 12월까지 열려 있는
자금지원사업을 통째로 놓친다.

여기는 기업마당과 반대로 **끝난 공고를 잘라낸다.** 기업마당 API 는 지금
열려 있는 것만 주지만 시청 게시판은 2009년부터 다 쌓여 있다. 기간이 지났거나
날짜가 없으면서 게재일이 재작년 이전이면 뺀다. 안 그러면 2017년 자금지원계획이
오늘 신청 가능한 것처럼 앉는다.

첨부는 .hwp 라 못 읽는데, 다행히 상세 페이지 「내용」 칸에 본문이 그대로 있다.

`.github/workflows/update-notices.yml` 은 일정을 껐다. 손으로 돌릴 때만 쓴다.

**갱신이 멈추면 카톡으로 온다.** 전에는 `last-run.txt` 에만 적혀서, 사람이
열어보지 않으면 아무 일도 안 일어났다 — 08-29 부터 닷새를 그렇게 흘렸다.
지금은 발송 cron 이 5분마다 도는 김에 그 파일을 같이 본다. 실패로 적혀
있거나, 「성공」인데 날짜가 오늘 것이 아니면(서버가 꺼져 있었으면 그렇게
된다) 관리자 카톡에 한 줄이 얹힌다. `.env` 의 `KAKAO_ADMIN_USER_ID` 가
받는 사람이고, 비어 있으면 cron 로그에만 남는다.

사장님에게는 안 보낸다. 「갱신이 멈췄어요」는 무슨 말인지도 모를 뿐더러
겁만 준다.

**두 cron 이 같은 잠금을 쓴다.** `~/bin/mars-notices.sh` 는 전에 `flock -n`
이라 잠금을 못 잡으면 그 자리에서 포기했고, 포기한 흔적이 아무 데도 안
남았다. 발송이 5분마다 같은 잠금으로 `git fetch` 를 하므로 06:11 에 겹치면
그날 수집이 통째로 사라진다 — 로그에 줄 하나 없이. 지금은 `-w 240` 으로
기다리고, 그래도 못 잡으면 `-E 99` 로 갈라내서 로그와 `last-run.txt` 양쪽에
적는다. 적어두지 않으면 어제 날짜의 「성공」이 그대로 남아서 오늘 수집이
없었다는 걸 알 길이 없다.

**런처 둘(`~/bin/mars-notices.sh`·`mars-kakao.sh`)은 저장소에 없다.**
서버에만 있다. 실제 로직은 `scripts/cron_update_notices.sh` 에 있고 런처는
클론을 맞춰 부르기만 해서 일부러 안 넣었다 — 두 벌이 되면 한쪽이 낡는다.
대신 서버가 죽으면 같이 사라진다는 것을 알고 있을 것.

### 갱신이 데이터를 깎았는지는 살아남은 공고로 잰다

`policy_data/guard.py` 가 마지막 관문이다. 여기서 걸리면 커밋을 안 한다.
**총량으로 재면 안 된다.** 기업마당은 지금 열려 있는 공고만 준다. 마감된
공고가 빠지면 딸린 서류도 같이 빠지는데, 총량만 보면 「깎였다」로 읽는다.

2026-08-29 부터 09-02 까지 닷새 동안 자동 갱신이 그래서 멈춰 있었다. 공고
11건이 마감됐는데 하필 서류가 많은 것들이라(평균 4.2개, 전체는 3.0개)
서류가 247 → 217(0.879)로 문턱 0.90 을 1.1%p 차이로 못 넘겼다. 살아남은
71건은 서류 201개를 그대로 갖고 있었는데도.

**한번 걸리면 스스로 못 빠져나온다.** 기준선을 올리는 `guard.py --save` 는
검사를 통과한 뒤에만 부르는데, 검사는 `update_notices.sh` 의 6/6 단계 안에
있다. 매일 같은 낡은 기준선과 재면서 격차만 벌어진다.

그래서 **양쪽에 다 있는 공고만 비교한다.** `baseline.json` 에 공고별
`[서류, 자격조건, 본문있음]` 을 적어둔다. 마감된 공고는 비교에서 빠지므로
만료에 안 흔들리고, 원래 잡으려던 사고 — pdftotext 가 없어 서류가 기본값
2개로 주저앉는 것(2026-08-17) — 은 오히려 더 정확히 걸린다. 합계로는 8건이
`11→2` 로 뭉개져도 6% 라 통과하므로, 공고 하나가 서류의 절반 넘게 잃은
것을 따로 센다.

**실패한 증거는 15분이면 지워진다.** 카톡 cron 이 5분마다 `git reset --hard`
로 클론을 맞춘다. 06:13 에 실패해 남은 워킹트리를 06:15 발송이 되돌려놓는다.
나중에 클론에서 guard 를 돌리면 통과해서 「돌려보니 되는데?」가 된다.
원인은 클론이 아니라 `mars-fit-cron-logs/` 의 그날 로그에서 볼 것.

### 무엇을 싣고 무엇을 버리는가

관문은 `collect.py` 의 `is_sosang()` 하나다. 전에는 같은 판정이 `collect`·
`extract`·`fetch_docs` 세 군데에 흩어져 있었고 실제로 서로 어긋나 있었다.

**화성시 공고는 낱말을 안 따진다.** 기업마당에 올라온 화성시 공고 17건 중
「소상공인」 계열 낱말이 든 것은 3건뿐이다. 나머지는 「중소기업 운전자금」,
「중소기업 특례보증」처럼 적혀 있는데 시가 직접 하는 자금·보증 사업은 관내
소상공인이 주 대상이다. 이걸 안 풀어줘서 **최종 목록에 자금·보증 공고가 한
건도 없었다** — 사장님에게 제일 급한 것이 돈인데.

**소기업·중소기업·창업기업은 낱말에 안 넣는다.** 재봤더니 각각 346·346·37건이
새로 걸리는데 「중소기업(소기업 포함)」 같은 상투구라 해외전시회·원전·레미콘이
대부분이다. 58건이 400건을 넘어가면 사장님이 목록을 못 읽는다.

**업종이 못 박힌 공고는 「확인필요」로 내린다**(`extract.py` 의 `INDUSTRY_ONLY`).
지우지 않는 이유는 제조업을 겸하는 사장님이 있을 수 있어서다. 이걸 안 하면
반도체 소부장 실증 공고가 카페 사장님 홈 1위에 89점 신청가능으로 뜬다.

`classify_target()` 을 그대로 쓰면 안 된다. 83건 중 76건이 restricted 로 나온다 —
「소상공인기본법 제2조에 따른 소상공인」까지 restricted 로 세기 때문이다.

## Vercel 함수는 12개까지다

Hobby 요금제 한도다. **13개가 되는 순간 빌드가 통째로 실패해서 셋 다 배포를
못 한다.** 한 사람이 함수를 하나 더 만들면 남의 화면까지 멈춘다.

    Build Failed
    No more than 12 Serverless Functions can be added to a Deployment
    on the Hobby plan.

실제로 한 번 막혔다. 상권분석을 외부 서버로 옮기면서 `api/commercial.py` 와
`api/foottraffic.py` 가 붙어 11 → 13이 됐다.

**함수를 새로 만들기 전에 세어볼 것.**

    ls api/*.py | grep -v '/_' | wc -l

**밑줄로 시작하는 파일은 함수로 안 센다.** `_shared.py`·`_store.py`·`_auth.py`
가 그래서 밑줄이고, 진단용 `_ping.py` 도 그렇게 내려두었다. 쓸 일이 생기면
이름만 되돌린다.

줄이는 길은 **같은 자원을 다루는 둘을 한 파일에서 메서드로 가르는 것**이다.
`/api/terms`(GET)와 `/api/terms/lookup`(POST)이 `api/glossary.py` 하나에
들어 있다. `vercel.json` 의 rewrites 가 둘 다 그리로 넘긴다.

팀을 만들면(Pro) 풀리지만 돈이 든다.

## OCR 은 Vercel 밖에서 돈다

easyocr 는 torch 까지 1.4GB 인데 Vercel 함수는 250MB 까지다. 그래서 사진을
읽는 일만 별도 서버가 맡는다.

    브라우저 ──HTTPS──▶ Vercel api/ocr.py ──HTTPS(터널)──▶ backend/ocr_server.py

`api/ocr.py` 는 중계만 한다. 브라우저가 그 서버를 직접 못 부르는 이유는
사이트가 https 인데 서버는 http 라 브라우저가 막기 때문이다(mixed content).

띄우는 법은 `scripts/run_ocr.sh` 위쪽 주석에 있다. 무료 터널이라 **켤 때마다
주소가 바뀐다.** 바뀌면 Vercel 환경변수 `OCR_BACKEND_URL` 을 고치고 재배포해야
한다. 환경변수는 저장소 소유자만 바꿀 수 있다(Hobby 요금제라 팀원 초대 불가).

**꺼져 있으면 온보딩이 알아서 직접 입력으로 간다.** `/api/health` 의 `ocr` 이
`OCR_BACKEND_URL` 을 실제로 보고 `{ready, detail}` 로 답하고, 온보딩이 열릴 때
한 번 물어본다(`fetchOcrReady`). 안 붙어 있으면 사업자등록증 화면을 아예 안
내고 바로 직접 입력이다 — 사진을 고르고 줄이고 올린 다음에야 안 된다는 말을
듣게 하지 않는다.

**주소를 넣고 재배포하면 코드를 안 고쳐도 사진이 도로 앞으로 온다.** 전에는
`ocr` 이 「미지원」이라고 박아둔 문자열이라 주소를 넣어도 계속 미지원이라고
답했다.

못 물어봤을 때(서버가 안 뜨거나 옛 배포라 `ocr` 이 문자열)는 `null` 로 두고
**사진 쪽을 그대로 둔다.** 점검이 안 됐다고 되는 기능을 감추면, 서버를 올린
날 아무도 사진을 못 쓴다.

**사진을 저장하지 않는다.** 메모리에서 읽고 필드만 돌려준다. 등록증 전문
(`raw_text`)도 안 돌려준다. 이 부분을 바꾸지 말 것.

**등록증을 읽는 길이 둘이라 두 곳 다 지켜야 한다.**

    api/ocr.py → backend/ocr_server.py    배포에서 도는 길
    backend/matching.py                    로컬 서버 (/api/business-registration)

뒤쪽이 원칙 밖에 있었다. `users/<기기>/uploads/` 에 원본을 쌓고, 등록증
전문을 기기 기록에 넣은 뒤 그대로 브라우저에 돌려줬다. 실제로 넉 달치
사진 넉 장이 연구실 서버에 남아 있었다. 지금은 `extract_from_bytes()` 로
메모리에서만 읽는다.

프론트는 `/api/ocr` 만 부르므로 이 길로는 아무도 안 들어온다. 그래도
고친 이유는 로컬 서버로 시연하는 날이 있어서다 — 그날 하루치가 쌓인다.

사진은 브라우저에서 긴 변 1600px 으로 줄여 보낸다. Vercel 이 본문을 4.5MB
까지만 받는데 base64 는 1.33배가 되어, 줄이지 않으면 요즘 폰 사진은 거의 다
튕긴다(`Onboarding.jsx` 의 `shrinkImage`).

### 주소 하나를 셋이 나눠 쓴다

`OCR_BACKEND_URL` 하나로 중계 셋이 다 나간다.

    api/ocr.py          사진 읽기
    api/commercial.py   상권분석 — 상가·학교·역·아파트
    api/foottraffic.py  유동인구 요약

**그래서 이 변수가 비면 상권분석도 같이 죽는다.** 실제로 그랬다. OCR 만
생각하고 안 넣어둔 동안 라이브에서 상권분석이 통째로 503 이었는데, 화면은
멀쩡히 떠서 아무도 몰랐다.

지금 이 주소는 `server_data/external_server.py`(pjrx.kr)를 가리킨다. 도메인이
있어서 켤 때마다 주소가 바뀌지 않는다. 위의 무료 터널·`backend/ocr_server.py`
는 그 서버가 없을 때 쓰는 길로 남아 있다.

시연 전 점검(`python3 scripts/preflight.py`)의 **[6] 외부 서버**가 이 셋을
실제로 찔러본다. 사진 한 장을 보내고, 동탄역 좌표로 상가를 세어본다.
상권분석이 죽어 있으면 대신할 것이 없어서 ❌ 로 잡고, OCR 은 직접 입력으로
넘어가므로 ⚠️ 로 잡는다.

## 카카오톡 알림

조건에 잘 맞는 공고가 새로 뜨면 사장님 카톡의 「나와의 채팅」으로 간다
(「나에게 보내기」). 친구에게 보내기는 안 쓴다 — 심사가 걸리고 필요하지도 않다.

    api/notify_kakao.py        켜기·끄기. refresh_token 을 Supabase 에 저장
    scripts/notify_kakao.py    실제 발송. 연구실 cron 이 공고 갱신 뒤에 부른다
    docs/supabase_schema.sql   kakao_notify · kakao_sent

**Vercel 이 아니라 연구실 서버가 보낸다.** 서버리스 함수는 요청이 와야
도는데 알림은 아무도 안 부를 때 나가야 한다.

### 받는 일과 보내는 일은 갈라져 있다

    06:11    ~/bin/mars-notices.sh   공고를 받아서 커밋·푸시
    5분마다   ~/bin/mars-kakao.sh     지금이 제 시각인 사람에게만 발송

전에는 공고를 받자마자 이어서 보냈다. 그러면 **새벽 6시에 카톡이 울린다.**
사장님이 설정에서 시각(분까지)과 요일을 고르므로(`sendHour`·`sendMinute`·
`sendDays`, 기본 아침 8시·매일) 발송은 자주 돌아야 한다. 실제로 보낼지는
`due_now()` 가 사람마다 판단한다.

**분 단위는 crontab 과 짝이다.** 화면이 5분 단위로 고르게 하고(`SEND_MINUTE_STEP`)
cron 이 `*/5` 로 돈다. 한쪽만 바꾸면 고를 수는 있는데 그 시각에 아무도 안 깨어난다.

**딱 맞을 때만 보내지 않는다.** 수집과 잠금을 나눠 써서 최대 3분까지 기다릴 수
있고 그 사이 분이 넘어간다. `SEND_WINDOW_MIN`(5분) 만큼을 창으로 본다 — 8시
0분으로 정해둔 사람을 8시 3분에 깨어나서도 보낸다.

**요일 번호를 조심할 것.** 화면은 JS `Date.getDay()`(일=0)로 저장하고
파이썬 `weekday()` 는 월=0 이다. 그냥 갖다 쓰면 「월요일만」을 골라둔
사장님에게 일요일에 간다. `js_weekday()` 로 변환한다.

제 시각이 아니면 **기준선도 안 잡고 건너뛴다.** 잡으면 처음 켠 날 8시가
오기 전에 「보냄」으로 적혀서 그날 공고를 통째로 놓친다.

손으로 돌릴 때는 `--force` 를 준다. `~/bin/mars-kakao.sh` 는 그냥 부르면
시각을 그대로 지킨다.

두 cron 은 **같은 잠금 파일**을 쓴다. 06:11 수집이 도는 중에 07:00 발송이
겹치면 `git reset` 이 반쯤 된 클론을 읽는다. 발송 쪽이 3분까지 기다린다.

**refresh_token 은 그 사람 카톡으로 메시지를 보낼 수 있는 자격증명이다.**
Supabase 에만 둔다. 저장소·로그·API 응답에 절대 싣지 않는다. 알림을 끄면
행을 지운다.

동의는 **이용 중 동의**다. 로그인 화면에 「카카오톡 메시지 전송」을 같이
띄우면 처음 온 사람이 광고 오는 줄 알고 체크를 뺀다. 「카톡 알림 받기」를
눌렀을 때만 `scope=talk_message` 로 한 번 더 물어본다.

돌아올 때 로그인과 같은 `?code=` 로 오므로 `state=notify` 로 갈라낸다.
**이걸 빼면 로그인 처리가 code 를 먼저 집어가고 알림은 안 켜진다.**

시연 때는 새 공고를 기다릴 수 없다. `--demo --user <id>` 로 제일 잘 맞는
1건을 그 자리에서 보낸다. **보낸 기록을 안 남기므로 몇 번이든 다시 된다** —
리허설에서 써버리면 정작 심사 때 안 오는 일이 없게.

**알림을 켠 직후에는 아무것도 안 보낸다.** 그 순간 조건에 맞는 공고가
스무 건쯤 되는데 그건 「새로 떴다」가 아니라 처음부터 있던 것이다. 하루
세 건씩 나가면 아흐레 동안 카톡이 온다. 첫 실행에는 지금 걸리는 것을 전부
「보냄」으로 적어두고 넘어간다. 인앱 종도 첫 방문에는 목록만 적어두고
아무것도 안 띄운다 — 같은 규칙이다.

카톡으로 가는 것은 두 가지다 — **조건에 맞는 새 공고**와 **세무 신고기한**.
한 통에 묶어 보낸다. 따로 보내면 아침에 두 번 울린다.

**세무는 첫 실행 기준선에서 빼둔다.** 공고는 켠 순간 스무 건이 쌓여 있어
기준선이 필요하지만, 세무는 날짜가 정해진 몇 건뿐이라 지금 걸린 것이 곧
알려야 할 것이다. 닷새 뒤 신고기한이 있는데 「처음이라」고 넘어가면 그
신고를 놓친다.

**같은 계산이 화면과 서버 두 곳에 있다.** 한쪽만 고치면 카톡은 왔는데
종에는 없거나 그 반대가 된다.

세무 프로필은 네 가지다 — `entity_type` · `vat_type` · `has_employee` ·
`withholding_half`. **`api/onboarding.py` 의 `PROFILE_KEYS` 에 없는 키는
조용히 버려진다.** 화면에만 저장되고 서버에는 안 올라가면, 새벽에 카톡이
서버 프로필로 매칭해서 화면과 다른 일정을 말한다.

    문턱(신청가능·점수)   utils/notifications.js  ←→  scripts/notify_kakao.py
    알림 설정             utils/notifySettings.js ←→  scripts/notify_kakao.py
    세무 기한 계산        utils/taxCalendar.js    ←→  scripts/notify_kakao.py 의
                                                     tax_events()
    신고 완료 열쇠        utils/taxDone.js 의      ←→  scripts/notify_kakao.py 의
                          taxDoneKey()                 pick_tax()

완료 열쇠는 「원본항목번호::기한」이다(`withholding-monthly::2026-09-10`).
편 뒤의 id 로 맞추면 안 된다 — 파이썬은 `-2026-9`, JS 는 `-2026-9` 로 같아
보이지만 해가 바뀌면 어긋난다. 한쪽만 고치면 화면에서 지운 신고가 카톡으로
계속 온다.

카톡으로 가는 것이 하나 더 있다 — **공고 자동 갱신이 멈췄다는 경고**다.
관리자에게만, 하루 한 번만 간다(`collection_stalled()`). 위의 「공고 자동
갱신」 절에 자세히 적어뒀다.

연구실 서버 `.env` 에 `SUPABASE_URL` · `SUPABASE_SERVICE_ROLE_KEY` ·
`KAKAO_CLIENT_ID` 가 있어야 한다. Vercel 에 넣은 것과 같은 값이다.
`KAKAO_ADMIN_USER_ID` 는 서버에만 있으면 된다 — Vercel 은 안 쓴다.

## 기기 저장이 원본이고 서버는 통로다

로그인이 필수가 아니다 — `/home` 에 가드가 없어서 카카오 로그인 없이도
서비스를 다 쓴다. 그래서 관심공고를 서버에만 두면 로그인 안 한 사람은 ★ 를
눌러도 아무 일이 안 일어난다. **화면은 언제나 localStorage 만 읽는다.**

로그인한 사람은 그 위에 서버 동기화가 얹힌다(`utils/userState.js`).

    관심공고 · 달력 메모 · 서류 진행 · 신청 완료   →  user_state 테이블
    세무 신고 완료 표시                            →  user_state 테이블
    알림 읽음 · 일정 탭 토글 · 본 공고 목록        →  그 기기에만

뒤쪽은 그 기기에서의 습관이라 따라다니면 오히려 이상하다.

**합치는 것은 로그인할 때 한 번뿐이고, 그 뒤로는 통째로 덮어쓴다.** 서버가
매번 합치면 사장님이 ★ 를 뺀 공고를 「이 기기에 없을 뿐」인지 「지운 것」인지
구분할 수 없어서 계속 되살아난다.

두 기기에서 동시에 고치면 나중 것이 이긴다. 항목마다 시각을 달면 더
정확해지지만 대회 규모에는 과하다.

**프로필이 서버에 안 올라가는 경로가 셋 있었다**(내 정보 편집·시연용 버튼).
새 경로를 만들면 `saveOnboarding`/`patchOnboarding` 을 부르는지 볼 것 —
카톡 알림이 새벽에 **서버 프로필**로 매칭해서, 안 올라가면 화면과 카톡이
다른 말을 한다.

## 두 벌로 존재하는 로직

세무일정 계산이 JS 와 파이썬 양쪽에 있다. **한쪽만 고치면 화면과 서버가
다른 날짜를 말한다.**

    project/src/utils/taxSchedule.js  ←→  policy_data/tax_schedule.py
    project/src/data/tax_calendar.json ←→ policy_data/tax_calendar.json

**데이터도 두 벌이다.** 두 파일은 바이트까지 같아야 한다. 맞춰주는 스크립트가
없어서 손으로 둘 다 쓴다 — 고친 뒤 `md5sum` 으로 확인할 것.

한 `exclusive_group` 안에서는 **기본이 되는 쪽에 `conditional` 을 붙이지
않는다.** 종합소득세가 그 모양이다(5/31 기본, 6/30 성실신고만 conditional).
원천세는 셋 다 conditional 이라, 직원 있는 사장님 달력에 원천세가 한 건도
안 떴다 — 1년에 열두 번 있고 늦으면 가산세가 붙는 신고인데 카톡에도 없었다.

파이썬 쪽이 원본이고 날짜 검증도 거기서 했다. 공휴일 목록(`HOLIDAYS`)은
음력이라 자동 계산이 안 된다. **연말에 다음 해를 손으로 채울 것.** 현재
2026·2027 등록됨.

### 직원이 있으면 원천세만이 아니다

「직원 있으세요?」를 물어놓고 원천세 하나만 보여주고 있었다. 실제로는 넷이
더 있고 셋은 늦으면 돈이 나간다.

    간이지급명세서 상반기분   07-31   미제출 0.25%    소득세법 제164조의3
    간이지급명세서 하반기분   01-31   미제출 0.25%    소득세법 제164조의3
    지급명세서·연말정산      03-10   미제출 1%       소득세법 제164조
    보수총액신고(고용·산재)   03-15   과태료 300만원  보험료징수법 제16조의10①

**간이지급명세서는 2027년 1월 지급분부터 매월로 바뀐다.** 여러 번 유예된
끝에 확정된 날짜다. 2026년까지만 반기별이므로 **2027년이 되면 두 건을 빼고
`recurrence: "monthly"` 한 건으로 바꿔야 한다.** 안 바꾸면 열두 번 낼 것을
두 번으로 알려준다.

**건강보험 보수총액신고는 안 넣었다.** 2025년부터 국세청에 간이지급명세서를
낸 사업장은 신고한 것으로 갈음한다. 우리가 간이지급명세서를 이미 알리고
있으므로, 건강보험까지 띄우면 안 해도 되는 일을 시키는 셈이다. 고용·산재는
갈음되지 않아서 따로 남는다.

연말정산 자체는 **법정 기한이 날짜가 아니다** — 「2월분 근로소득을 지급할
때」다(소득세법 제137조). 날짜를 지어낼 수 없어서 3/10 지급명세서 항목의
`caution` 에 적어뒀다.

## 담당

| 사람 | 영역 |
|---|---|
| 전서희 | LLM 전부 — 챗봇, 체크리스트, 공고 요약, 온보딩 LLM, 서류준비 창 |
| 전성현 | 상권분석, 달력, OCR |
| 임대윤 | 세무일정, 관심공고, 알림, 데이터 수집, 배포 |

겹치는 파일은 `project/CLAUDE.md` 에 적어뒀다.

2026-08-23 에 OCR 과 관심공고를 맞바꿨다. 원래 관심공고(성현) → 알림(대윤)
순서가 걸려 있었는데, 둘 다 대윤이 맡으면서 그 대기가 없어졌다. 대신
달력의 「관심공고만 표시」(성현)가 관심공고 뒤로 온다 — `utils/favorites.js`
의 `listFavorites()` 만 부르면 되므로 지금 바로 붙일 수 있다.

## 숫자를 지어내지 않는다

화면에 출처 없는 숫자를 두지 않는 것이 이 서비스의 원칙이다. 매출·재방문율
같은 걸 그렸다가 전부 지웠다 — POS 도 카드매출도 연동한 적이 없었기
때문이다. 하단에 「목업」이라고 적어도, 그 줄을 읽은 사람은 다른 숫자까지
의심한다.

상권분석의 모든 항목(음식점·카페·학원·학교·역·아파트 단지)은 실제 데이터다.
유동인구 예측은 이 실측값을 가중합산한 추정치다
(`project/src/components/sections/CommercialAnalysisView.jsx`).

## 대회가 끝나면 지울 것

- `project/src/utils/demoMode.js` 와 온보딩의 `DemoSkip` — 시연용 프로필
