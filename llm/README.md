# LLM / RAG

담당: 작성자

- 임베딩 모델·LLM 모델 후보 비교 (10개 내외), RAG 적용 전후 결과 차이를 발표 자료로
- 매칭 엔진의 JSON 출력을 입력으로 받는다. 출력 양식은 이쪽에서 정의해 매칭 담당에게 전달
- 소득분위 추정(가족 구성·소득 자연어 입력 → 통계청 기준 추정)
- 계획 없는 사용자용 업종 유도 문답
- 행정 용어 쉬운 설명 생성 (용어 DB에 없으면 생성 후 DB에 자동 추가)
